/**
 * MyProfilePage
 * Página para ver el perfil propio (read-only)
 * Route: /profile
 */

import { MyProfile } from '@/features/profile';

const MyProfilePage = () => {
  return <MyProfile />;
};

export default MyProfilePage;

